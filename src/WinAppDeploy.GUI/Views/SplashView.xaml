<UserControl x:Class="WinAppDeploy.GUI.Views.SplashView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding SplashViewModel, Source={StaticResource ViewModelLocator}}"
             >
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <controls:ProgressRing Foreground="{DynamicResource AccentColorBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" HorizontalContentAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Checking Windows 10 SDK..." HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
